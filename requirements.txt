androguard==4.1.4
frida==17.16.2
frida-tools==14.10.4

# lxml은 androguard가 함께 설치해 주지만, 테스트에서 직접 import하므로 명시해 둔다
# (androguard.get_android_manifest_xml()이 lxml Element를 반환)
#
# 4.8.0으로 고정하지 않는다: lxml은 C 확장이라 미리 빌드된 wheel로 설치되는데,
# 4.8.0(2022)에는 Python 3.12용 wheel이 없다. 그래서 Windows + Python 3.12로 받은
# 팀원은 pip이 소스 컴파일을 시도하다 C 컴파일러가 없어 `Failed building wheel for
# lxml`로 설치 자체가 막혔다(2026-08 실측). 하한만 두면 각 환경에 맞는 최신 wheel이
# 설치된다. androguard 4.1.4는 lxml 6.x와도 정상 동작함을 확인했다(6.1.1로 검증).
lxml>=4.8.0

# 대시보드 (7~8주차). A의 app.py / common.py / views/*가 전부 이걸 쓴다.
# 7주차 계획에는 A가 PR에 포함하기로 돼 있었는데 골격 PR(#6)에 빠져 있어서
# B와 D가 각자 추가했고, 그 결과 이 파일에 두 번 들어가 있었다(8주차 Day1 정리).
# 이게 없으면 새로 받은 사람이 `streamlit run app.py`부터 막힌다.
# 화면 렌더링 테스트(tests/test_static_view_render.py)도 이 패키지를 쓴다.
# altair/pandas는 streamlit이 함께 설치한다. views/*가 `width="stretch"` 등
# 최신 API를 쓰므로 이 버전 미만으로 내리면 안 된다.
streamlit==1.61.1

# 테스트용 (6주차 B — 어댑터 단위 테스트 + 통합 스키마 검증)
pytest==9.1.1
# jsonschema: 원래 3.2.0였으나 아래 streamlit/altair가 더 높은 버전을 요구해 4.26.0으로
# 올림. B의 검증 코드는 Draft7Validator(단일 스키마) 하나만 쓰고 $ref/RefResolver를
# 안 써서 4.x에서도 API 동일하게 동작함(확인 완료). Draft-07 검증 시맨틱도 그대로.
jsonschema==4.26.0

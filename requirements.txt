androguard==4.1.4
frida==17.16.2
frida-tools==14.10.4

# lxml은 androguard가 함께 설치해 주지만, 테스트에서 직접 import하므로 명시해 둔다
# (androguard.get_android_manifest_xml()이 lxml Element를 반환)
lxml==4.8.0

# 7주차 대시보드. A의 app.py / common.py / views/*가 전부 이걸 쓴다.
# 7주차 계획에는 A가 PR에 포함하기로 돼 있었는데 골격 PR(#6)에 빠져 있어서
# B가 추가한다 — 이게 없으면 새로 받은 사람이 `streamlit run app.py`부터 막힌다.
# 화면 렌더링 테스트(tests/test_static_view_render.py)도 이 패키지를 쓴다.
streamlit==1.61.1

# 테스트용 (6주차 B — 어댑터 단위 테스트 + 통합 스키마 검증)
pytest==9.1.1
jsonschema==3.2.0

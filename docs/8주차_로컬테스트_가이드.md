# 8주차 로컬 테스트 가이드 (WSL + Windows 에뮬레이터)

**작성 기준**: 2026-08-13, 브랜치 `feature/8w-b-cleanup`
**대상**: 8주차 계획수정 PDF의 "실제 로컬에서 환경 구축 및 실행 → apk 넣어서 오류 발생 확인 및 오류 해결"
**샘플 취급**: 분석용 APK는 **저장소에 절대 커밋하지 않음**. `.gitignore`에 `*.apk` 등록됨. 샘플은 `~/samples/`에만 둘 것

---

## 0. 이 PC의 구조 — 먼저 이해할 것

- 코드·파이썬·pytest = **WSL(Ubuntu 22.04)**
- 에뮬레이터 = **Windows** 에서만 실행 가능 (WSL2에 `/dev/kvm` 없음)
- 따라서 **WSL의 파이썬이 Windows의 에뮬레이터를 조작**해야 하고, 그 통로가 `adb-bridge`

```
[WSL]  src/main.py ─ adb 클라이언트 ─┐
                     frida ──────────┤ 127.0.0.1:5037
                                     ↓
                        adb-bridge (TCP 중계)
                                     ↓
[Windows] adb 서버 (172.25.144.1:5037) → 에뮬레이터
```

- `frida`는 `ADB_SERVER_SOCKET` 환경변수를 보지 않고 **항상 127.0.0.1:5037**에 붙음 → 환경변수만으로는 해결 안 되고 중계가 필요한 이유

---

## 1. 설치 완료된 것 (2026-08-13 기준, 재설치 불필요)

| 도구 | 위치 | 버전 |
|---|---|---|
| JRE | `~/tools/jre` | 17 |
| apktool | `~/tools/bin/apktool` | 3.0.3 |
| jadx | `~/tools/bin/jadx` | 1.5.6 |
| adb (Linux) | `~/tools/bin/adb` | 37.0.1 |
| adb-bridge | `~/tools/bin/adb-bridge` | — |
| frida (python) | pip `--user` | 17.16.2 |
| frida-server | `~/tools/frida-server-17.16.2-android-{x86,x86_64}` | 17.16.2 |

- 전부 `~/tools` 와 pip `--user` 안에만 설치됨. 되돌리려면 `rm -rf ~/tools`
- `sudo` 불필요. 시스템 패키지는 건드리지 않음

매 터미널마다:

```bash
cd ~/ecops_2325
source scripts/wsl_env.sh
```

---

## 2. 팀 공용 AVD 사양 — 샘플 선택의 제약

현재 AVD: **`Pixel_4` / API 29 / google_apis / x86(32비트)**

```
ro.product.cpu.abilist = x86        # x86 단독. ARM 변환(libhoudini) 없음
PlayStore.enabled      = false      # google_apis 이미지 -> adb root 가능 (tcpdump 캡처에 필수)
```

여기서 나오는 **샘플 선택 기준 2가지**:

| 조건 | 통과 | 실패 시 |
|---|---|---|
| 네이티브 라이브러리 | `lib/` 없음(DEX 전용) 또는 `lib/x86` 포함 | `INSTALL_FAILED_NO_MATCHING_ABIS` |
| minSdk | ≤ 29 | 설치 거부 |

- `arm64-v8a` / `armeabi-v7a` / `x86_64` **전용** APK는 이 AVD에 설치 불가
- 안드로이드 악성코드는 상당수가 DEX 전용(네이티브 라이브러리 없음)이라 통과 가능. 다만 상용 패커(`libjiagu` 등)를 쓴 샘플은 대부분 ARM 전용이라 탈락
- **정적 분석은 ABI/API와 무관하게 항상 가능** → 탈락한 샘플도 "정적 전용"으로는 쓸 수 있음

받은 APK를 한 번에 거르는 스크립트:

```bash
python3 scripts/check_apk_compat.py ~/samples/*.apk
```

출력 예:

```
=== sample_a.apk ===
  패키지     : com.example.malware
  minSdk     : 21  (기기 API 29)
  네이티브ABI: 없음 (DEX 전용)
  주목 권한  : INTERNET, READ_SMS, RECEIVE_SMS, SYSTEM_ALERT_WINDOW
  판정: [설치 가능]  동적 분석 볼거리: 좋음 (주목 권한 5개)
```

---

## 3. 샘플 APK 확보

### 3-1. 악성 샘플 — MalwareBazaar

1. https://auth.abuse.ch/ 에서 **무료 계정 생성 → Auth-Key 발급** (API·다운로드 모두 인증 필수. 인증키 없이 호출하면 `{"error": "Unauthorized"}`)
2. https://bazaar.abuse.ch/browse/ 에서 검색
   - **`file_type: apk`로 반드시 필터.** `elf` 태그는 Linux/IoT 계열(Mirai 등)이라 안드로이드가 아님
   - 태그로 계열 검색도 가능: `SpyNote`, `Cerberus`, `Hydra`, `Ermac`, `FluBot`, `Joker`
3. 다운로드 zip 비밀번호는 공통으로 **`infected`**

API로 받는 경우:

```bash
KEY=발급받은_키
# 태그로 목록 조회
curl -s -X POST https://mb-api.abuse.ch/api/v1/ \
     -H "Auth-Key: $KEY" \
     -d "query=get_taginfo" -d "tag=apk" -d "limit=50" | python3 -m json.tool | less

# sha256으로 다운로드
curl -s -X POST https://mb-api.abuse.ch/api/v1/ \
     -H "Auth-Key: $KEY" \
     -d "query=get_file" -d "sha256_hash=<해시>" -o ~/samples/sample.zip
cd ~/samples && 7z x -pinfected sample.zip     # 또는 unzip -P infected
```

**어떤 계열을 고를지** — 우리 AVD(x86, API 29)와 동적/네트워크 모듈 기준:

| 우선순위 | 계열 | 이유 |
|---|---|---|
| 1순위 | **SpyNote / AhMyth 계열 RAT** | 순수 Java(DEX 전용)가 많아 x86 설치 가능성이 높고, C2로 주기적 통신 → 네트워크 모듈에 실제로 잡힘. SMS·연락처·녹음 권한이 많아 정적 권한 표도 채워짐 |
| 2순위 | **Cerberus / Hydra / Ermac** (뱅킹) | Anubis와 같은 계열이라 3~6월 분석 경험과 직접 비교 가능. 접근성+오버레이+SMS 조합이라 "고위험 권한 조합" 강한 지표가 실제로 뜨는지 검증됨. 다만 패커를 쓴 변종은 ARM 전용이라 탈락할 수 있음 |
| 3순위 | **Joker / FluBot 드로퍼** | 드로퍼는 본체가 작아 설치는 잘 되지만, 실행 직후 페이로드를 받아오지 못하면 관측되는 게 적을 수 있음 |

- 실무적으로는 **후보 5~10개를 받아 `check_apk_compat.py`로 거른 뒤** 통과한 것 중 주목 권한이 많은 것을 고르는 편이 빠름
- 팀 내 중복 금지: 백소정·김은아가 이미 가져간 파일과 겹치지 않게 확인할 것. `anubis.apk` / `syssecapp.apk`는 PDF에서 제외 대상

### 3-2. 정상 앱 (대조군)

- APKPure / APKMirror에서 받거나, 에뮬레이터의 시스템 앱을 추출:

```bash
adb shell pm list packages | grep -i calendar
adb shell pm path com.google.android.calendar
adb pull /data/app/.../base.apk ~/samples/calendar.apk
```

- 권한 구성이 대비되도록 2종(계산기류 1 + 메신저/SNS류 1)을 고르면 점수 차이가 잘 드러남

### 3-3. 취급 규칙 (8주차 계획 1항)

- 샘플 실행 **전** 스냅샷 저장, 실행 후 복원 (감염 상태 누적 방지)
- 저장소에 커밋 금지. `~/samples/` 밖으로 옮기지 말 것

---

## 4. 1단계 — 정적 분석만 (에뮬레이터 불필요)

가장 먼저 이것부터. 에뮬레이터 없이 되고 실패해도 원인이 좁음.

```bash
cd ~/ecops_2325
source scripts/wsl_env.sh
python3 src/main.py ~/samples/<샘플>.apk --output output/report_<이름>.json
```

- 에뮬레이터가 없으면 dynamic/network가 `분석 실패`로 찍히는 것이 **정상 동작**(부분 리포트 정책)
- `정적 분석  분석 성공`만 확인되면 통과
- 라이브러리 DEBUG 로그는 기본적으로 꺼져 있음. 필요하면 `--verbose`

---

## 5. 2단계 — 에뮬레이터 + adb 연결

### 5-1. 에뮬레이터 실행 (반드시 **Windows 터미널**에서)

> WSL에서 `emulator.exe`를 실행하면 `could not load PC BIOS 'bios-256k.bin'`로 실패하고 좀비 프로세스가 남음(실측 확인). 반드시 Windows(PowerShell) 또는 Android Studio Device Manager에서 띄울 것.

PowerShell 창 ①:

```powershell
cd $env:LOCALAPPDATA\Android\Sdk\emulator
.\emulator.exe -avd Pixel_4 -writable-system
# 창 없이: -no-window / 감염 누적 방지: -no-snapshot-save
```

### 5-2. Windows adb 서버 열어두기

PowerShell 창 ②:

```powershell
cd $env:LOCALAPPDATA\Android\Sdk\platform-tools
.\adb.exe kill-server
.\adb.exe -a -P 5037 nodaemon server
```

- `-a` = 모든 인터페이스 바인드(WSL 접근의 핵심). 이 창은 켜 둔 채 유지

### 5-3. WSL에서 중계 켜기

```bash
source ~/ecops_2325/scripts/wsl_env.sh
adb kill-server      # WSL 로컬 서버가 떠 있으면 5037 포트 충돌
adb-bridge &
adb devices          # emulator-5554  device
```

---

## 6. 3단계 — 에뮬레이터 사전 준비 (tcpdump / frida-server)

**이 단계를 건너뛰면 7주차에 C가 멈춘 지점(tcpdump 에러)에서 똑같이 멈춤.**
`capture.py`가 `/data/local/tmp/tcpdump`를 하드코딩해 확인하고, `scenario_capture.py`가 push 없이 호출하기 때문에 없으면 동적·네트워크가 **둘 다** 실패함.

```bash
cd ~/ecops_2325
bash scripts/prepare_emulator.sh ~/samples/<샘플>.apk
```

하는 일: 기기 확인 → `adb root` → tcpdump 준비 → ABI에 맞는 frida-server push·실행 → APK 설치(`-g`로 권한 부여).

수동으로 하려면:

```bash
adb root
adb shell "cp /system/bin/tcpdump /data/local/tmp/tcpdump && chmod 755 /data/local/tmp/tcpdump"
adb push ~/tools/frida-server-17.16.2-android-x86 /data/local/tmp/frida-server
adb shell chmod 755 /data/local/tmp/frida-server
adb shell "nohup /data/local/tmp/frida-server >/dev/null 2>&1 &"
frida-ps -U | head
adb install -r -g ~/samples/<샘플>.apk
```

**tcpdump가 이미지에 없을 때 대안**: 에뮬레이터를 `-tcpdump` 옵션으로 띄우면 호스트에서 직접 pcap을 뜰 수 있음(파이프라인 자동화는 안 되지만 pcap 확보는 됨).

```powershell
.\emulator.exe -avd Pixel_4 -writable-system -tcpdump C:\temp\capture.pcap
```

---

## 7. 4단계 — 전체 파이프라인

```bash
python3 src/main.py ~/samples/<샘플>.apk \
    --output output/report_<이름>.json --observe-sec 8
```

옵션: `--work-dir` / `--hooks-js` / `--output-pcap` / `--observe-sec` / `--verbose`
시나리오는 `LAUNCH_ONLY` 고정(앱 실행 후 대기). `login_flow`/`permission_request`는 좌표가 플레이스홀더라 아직 사용 불가.

산출물:
- `output/report_<이름>.json` — 통합 리포트
- `output/capture.pcap` — 캡처 패킷
- `output/dynamic_report_launch_only_<시각>.json` — 후킹 이벤트

동적 분석만 따로:

```bash
python3 src/dynamic_analyzer/run_test.py <패키지명> 8
```

---

## 8. 5단계 — 대시보드

```bash
streamlit run app.py        # http://localhost:8501
```

기기 없이 화면만 확인:

```bash
streamlit run demo_risk.py     # 판정 카드 + 종합 위험도 (5가지 상태 전환)
streamlit run demo_static.py   # 정적 탭
```

회귀 테스트:

```bash
python3 -m pytest -q           # 154개
```

---

## 9. 화면 구조 (8주차 개편 — 계획수정 PDF 반영)

**분석 상태(Status) ≠ 보안 판정(Verdict)** 를 코드 수준에서 분리함.

```
              APK 보안 분석 결과
            🟠 의심 (SUSPICIOUS)
           종합 위험도  64 / 100
─────────────────────────────────────
분석 상태  (앱의 안전 여부가 아니라 실행 성공 여부)
  정적 분석  ✅ 분석 성공   동적 분석  ✅ 분석 성공   네트워크  ✅ 분석 성공
─────────────────────────────────────
[탭] 정적 / 동적 / 네트워크 / 종합 위험도
   각 탭 상단: 분석 상태 + 위험 지표 두 줄
   종합 탭: 위험 지표 표 → 모듈별 위험도 → 게이지 → 기여도 → 판정 근거 → 주의 문구
```

- 상태 라벨: `정상/실패` → **`분석 성공/부분 성공/분석 실패/시간 초과`**
- 판정 구간: **0–29 정상 / 30–59 주의 / 60–79 의심 / 80–100 고위험**
- **"악성"은 점수만으로 주지 않음**: 종합 80점 이상 **그리고** 강한 지표 3개 이상을 둘 다 충족할 때만. 화면에 "악성으로 표시하지 않은 이유"를 그대로 노출함
- 모든 화면·CLI 하단에 고지 문구 고정

---

## 10. 트러블슈팅

| 증상 | 원인 | 조치 |
|---|---|---|
| `'apktool' 실행 파일을 찾을 수 없음` | PATH 미설정 | `source scripts/wsl_env.sh` |
| `adb devices`가 비어 있음 | Windows adb 서버 미기동 / 중계 미실행 | 5-2, 5-3 확인. `pgrep -f adb-bridge` |
| `adb-bridge` 바인드 실패 | WSL에 로컬 adb 서버 존재 | `adb kill-server` 후 재실행 |
| `Frida 연결 실패: device not found` | frida-server 미실행 | `adb shell pidof frida-server` 확인 |
| `unable to connect to 127.0.0.1:27042` | frida 버전 불일치 | 파이썬 frida와 frida-server 둘 다 17.16.2인지 확인 |
| `TcpdumpCaptureError: 기기에 tcpdump가 없고...` | `/data/local/tmp/tcpdump` 없음 | 6단계 수행 |
| `tcpdump가 기기에서 시작되지 않았습니다` | root 아님 | `adb root` 후 `adb shell whoami` |
| `INSTALL_FAILED_NO_MATCHING_ABIS` | APK ABI ≠ x86 | `check_apk_compat.py`로 사전 확인. 해당 샘플은 정적 전용 |
| `could not load PC BIOS` | WSL에서 emulator.exe 실행 | Windows에서 실행. 잔여 `qemu-system-x86_64-headless`는 작업 관리자에서 종료 |
| `Another emulator instance is running` | 이전 인스턴스 잔존 | 작업 관리자에서 qemu 종료 후 재시도 |

---

## 11. 남은 과제

- **점수 상수 실샘플 검증**: 구간·가중치·상한을 조정해 "취약 APK = 의심(60~79)", "정상 앱 = 정상(0~29)"이 되도록 맞췄으나, 근거가 합성 케이스임. 실제 정상 앱 2개 + 악성 2개로 돌려 재조정 필요 (`tests/test_risk_aggregator.py`의 케이스를 실측값으로 교체)
- `login_flow` / `permission_request` 시나리오 좌표 — 실제 대상 앱 확정 후 채워야 함
- outer timeout 트립 시 adb/frida 좀비 프로세스 정리 (6주차부터 이월)
